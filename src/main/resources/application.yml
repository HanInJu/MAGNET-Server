spring:
  profiles:
    active: hoyoon

---

spring:
  profiles:
    group:
      "hoyoon": "hoyoondb, common"
      "heather": "heatherdb,common"
      "dev": "devdb,common"

---

spring:
  config:
    activate:
      on-profile: "devdb"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://devcomdb.cgcaudjm5set.ap-northeast-2.rds.amazonaws.com:3306/MAGNET?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: admin
    password: DevCommunityKo82

---

spring:
  config:
    activate:
      on-profile: "heatherdb"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/petComes?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: Heather
    password: HeatherMySQL

---

spring:
  config:
    activate:
      on-profile: "hoyoondb"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/magnet?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: hoyoon
    password: DatabasePassword
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true
#  cors:
#    allowed-origins: 'http://localhost:3000'
#    allowed-methods: GET,POST,PUT,DELETE,OPTIONS
#    allowed-headers: '*'
#    max-age: 3600

---

spring:
  config:
    activate:
      on-profile: "common"
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    open-in-view: false
#    properties:
#      hibernate:
#        format_sql: true
#        use_sql_comments: true
#    hibernate:
#      ddl-auto: none
#    show-sql: true


